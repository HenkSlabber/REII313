#ifndef SETTINGS_H
#define SETTINGS_H

#include <QMainWindow>

class settings : public QMainWindow
{
    Q_OBJECT

public:
    settings(QWidget *parent = nullptr);
    ~settings();

private:

private slots:

};

#endif // SETTINGS_H
