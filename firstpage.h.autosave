#ifndef FIRSTPAGE_H
#define FIRSTPAGE_H

#include <QMainWindow>

class firstpage : public QMainWindow
{
    Q_OBJECT

public:
    firstpage(QWidget *parent = nullptr);
    ~firstpage();

private:

private slots:

};
#endif // UNOLITE_H
